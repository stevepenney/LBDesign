# Flask Core
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
Werkzeug==3.0.1

# Database Drivers
PyMySQL==1.1.0          # For MySQL
# pyodbc==5.0.1         # For SQL Server - install separately if needed
cryptography==41.0.7    # Required by PyMySQL

# For development (SQLite support is built into Python)
# No additional driver needed for SQLite

# Utilities
python-dotenv==1.0.0

# Optional but recommended for production
gunicorn==21.2.0        # Production WSGI server (Linux/Mac only)
